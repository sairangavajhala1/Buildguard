# Server
PORT=3000
HOST=127.0.0.1

# Logging
LOG_LEVEL=info

# Payments
# Copy your dodo payments API key into a local .env file (gitignored).
# Do NOT put a real key in this file — it is committed to the repo.
DODO_PAYMENTS_API_KEY=