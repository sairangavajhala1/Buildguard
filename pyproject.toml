[project]
name = "buildguard-backend"
version = "0.1.0"
description = "Buildguard: Dodo escrow backend, audit engine, CFO dashboard and HITL voice alerts"
requires-python = ">=3.9"
dependencies = [
    "dodopayments>=1.115.0",
    "fastapi>=0.115.0",
    "pydantic>=2.7.0",
    "python-dotenv>=1.0.0",
    "python-dotenv>=1.2.1",
    "requests>=2.31",
    "uvicorn>=0.30.0",
]

[dependency-groups]
dev = [
    "httpx>=0.27.0",
    "pytest>=8.0.0",
    "pytest-cov>=5.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
